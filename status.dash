#!/bin/dash
while true
do
    string="⚡`acpi | rg '(..%)|(..:..:..)' -o |tr '\n' ' '`|"
    string="${string} 🔈`pamixer --get-volume-human` |"
    string="${string} `date '+%a %m/%d %I:%M'`"
    xsetroot -name "${string}"
    sleep 1m
done
